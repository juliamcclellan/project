class Entry
  
  initialize(time_entry, name)
    @time_entry = time_entry
    @name = name
  end
  
  def add_collaborator(collaborator)
    @collaborator = collaborator
  end
  
  def entryString
    "#{name} made an entry at #{time_entry}"
  end

end
